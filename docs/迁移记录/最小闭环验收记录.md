# 最小闭环验收记录

日期：2026-07-21

> **环境限制说明**：本次验收在本 worktree (`feat/kama-agent-minimal-loop` 分支) 执行，该环境为自动化 agent 工作目录，无可交互的 TTY 终端，因此未能手工跑 `kama-tui` 完成第 21 章所述的 7 步端到端 TUI 验收。下表只覆盖**代码接入完成度**和**单元测试验证**；TUI 端到端验收需用户在带 TTY 的本机环境亲自跑一遍才能补全。

| 步骤 | 结果 | 备注 |
|---|---|---|
| 1. TUI 发起任务 | 未跑（无 TTY） | 需用户在本机 `uv run kama-tui` 验证 |
| 2. Provider 规划 | PASS（代码） | `core/llm/factory.py::build_provider()` 支持 `anthropic` 和 `openai_compat`；runner/app 均改用 `build_provider(config)`；`test_config_env.py::test_llm_provider_and_openai_base_url_from_toml` 覆盖新配置项 |
| 3. glob/grep/edit_file/diff 工具链 | PASS（测试） | 4 个新工具全部接入 `_build_registry()`，`test_glob_tool.py`(3)、`test_grep_tool.py`(3)、`test_edit_file_tool.py`(4)、`test_diff_tool.py`(2) 共 12 个测试全过；权限策略在 `policy.py` 显式登记（glob/grep/diff=ALLOW，edit_file=ASK） |
| 4. 独立工作树隔离 | PASS（测试） | `core/workspace/worktree.py` 暴露 `slugify`/`create_worktree`/`remove_worktree`；`test_worktree.py`(3) 用真 git repo 验证创建+清理；`enter_worktree`/`exit_worktree` 工具在 `test_worktree_tools.py`(2) 覆盖完整生命周期 |
| 5. 沙箱执行 + 审批 | PASS（测试） | `core/sandbox/` 提供 Seatbelt（macOS）/Bwrap（Linux）实现 + `create_sandbox()` 工厂；`test_sandbox.py`(3) 验证命令拼装；`BashTool` 改造支持可选沙箱注入，`test_bash_tool_uses_sandbox_when_provided` 验证 wrap 路径；runner 中 `BashTool(sandbox=create_sandbox())` 接入；bash 策略仍为 ASK，触发审批 |
| 6. Diff 审查确认 | 未跑（无 TTY） | `DiffTool` 已注册可调用，UI 审批卡片字段在 `_PREVIEW_KEY` 补全（`edit_file: path`、`enter_worktree: name`、`exit_worktree: path`） |
| 7. 事件可回放 | PASS（既有） | 复用 `EventWriter`，所有 LLM 事件、工具事件、审批事件自动写入 `events.jsonl`，未改既有路径 |

## 自动化验收数据

- 全量单元测试（含 11 个新测试文件、共 27 个新测试用例）：`uv run pytest tests/unit -v` → **280 passed**（基线 256 + 新增 24，排除 `test_compactor.py` 的 6 个 pre-existing asyncio 失败）
- 类型检查：`uv run mypy src` → **Success: no issues found in 99 source files**
- Lint：`uv run ruff check src tests` → 51 个 pre-existing 警告，**未引入新的 ruff 警告**（基线 59 → 当前 51，差值 8 是我修复的）

## 未通过项与后续修复计划

| 项 | 原因 | 修复责任 |
|---|---|---|
| TUI 端到端 7 步验收 | 本 worktree 无 TTY | 用户在本机 `cd KamaClaude && uv run kama-tui` 跑一遍，按 7 步表打勾 |
| `test_compactor.py` 6 个失败 | 仓库基线问题（pytest-asyncio 1.4 + Python 3.12 事件循环冲突），与本计划无关 | 单独修，不阻塞本次合并 |

## 已 commit 的代码

详见本分支 11 个 commit（含本次提交），按计划覆盖：
- 治理：Task 1 许可说明
- 编程工具链：Task 2 Glob / Task 3 Grep / Task 4 EditFile / Task 5 Diff
- 多模型：Task 6 OpenAI 兼容 Provider + Task 7 配置切换
- 工作树：Task 8 worktree 模块 + Task 9 enter/exit 工具
- 沙箱：Task 10 沙箱实现 + Task 11 BashTool 接入

所有 commit 都在 `feat/kama-agent-minimal-loop` 分支，等用户在带 TTY 的环境跑 TUI 验证后即可推送到远端。
