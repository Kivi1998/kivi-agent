# 最小闭环验收记录

> **分支基线**：`integration/wave1`（从 `docs/agent-plans` 切出，按 minimal-loop → D → E → C 顺序合并 4 个子包）
> **验收日期**：2026-07-21
> **记录人**：Mavis（自动化 agent）+ 用户手动跑通 TUI 7 步

## 一句话总结

mewcode 最小闭环（编程工具链 + 多模型 Provider + 工作树隔离 + 命令沙箱）+ 包 D（PermissionMode + Hooks）+ 包 E（压缩熔断 / 检查点 / 长期记忆）+ 包 C TDD 计划已全部合入 `integration/wave1`。**329 个单元测试全过、mypy 0 issue / 111 文件**、用户 TUI 跑通 `pwd` 任务 1 步 tool_use + 1 步 text 响应闭环。

---

## 7 步端到端 TUI 验收（用户本机执行）

> 用户在个人终端 `cd KamaClaude && uv run kama-tui`，发送目标 `当前目录是什么 pwd`，`run_id=20260721-155022-7db3e0`。
> 证据来源：`~/.kama/traces/daemon.jsonl`（78 条事件）、`~/.kama/sessions/sess-*/`。

| 步骤 | 描述 | 结果 | 证据 |
|---|---|---|---|
| 1 | TUI 发起任务 | **PASS** | `run.started` 事件 `goal="当前目录是什么 pwd"`；`step.started step=1` |
| 2 | Provider 规划（Anthropic 兼容走 deepseek /anthropic） | **PASS** | `llm.model_selected model="deepseek-v4-pro" strategy="static"`；trace 里 `api_call` payload 的 `model` 字段 = `deepseek-v4-pro`；HTTP 走 `ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic`（由 anthropic Python SDK 0.105.2 原生读取，无代码改动） |
| 3 | 工具调用（bash 沙箱） | **PASS** | step 1 `tool_use id=call_00_mKeuqpHjvLUlBnzMxFVP3593 name=bash input={command:pwd}`；`tool.call_started` 事件；沙箱通过 `BashTool(sandbox=create_sandbox())` 注入（runner 装配） |
| 4 | Tool result 回灌 + 二次 LLM 推理 | **PASS** | `tool.call_finished output=/Users/kivi/Documents/agent系统/Kama/KamaClaude`；step 2 `text` 响应完整回写 |
| 5 | 审批（bash 策略=ASK，触发 PermissionManager） | **PASS（自动化）** | 当前 `BashTool` 策略为 `ASK`，runner 装配 `PermissionManager(timeout_s=60.0)`，本 run 由 TUI 客户端代为同意（用户在 TUI 内通过审批卡片） |
| 6 | 终态文本 | **PASS** | step 2 `stop_reason=end_turn` `text="当前工作目录是：\n/Users/kivi/Documents/agent系统/Kama/KamaClaude"` |
| 7 | 事件可回放 | **PASS** | 全部事件落 `~/.kama/traces/daemon.jsonl`（含 `run.started` / `step.started` / `llm.model_selected` / `llm.usage` / `tool.call_started` / `tool.call_finished` / `run.finished`），可重放 |

### 关键 usage 数据（佐证缓存机制工作）

```json
{
  "input_tokens": 135,
  "output_tokens": 63,
  "cache_read_input_tokens": 3584,
  "cache_creation_input_tokens": 0,
  "context_pct": 0.000675
}
```

`cache_read_input_tokens=3584` 说明 deepseek /anthropic 端支持 prompt cache，与 Anthropic 原生缓存语义兼容，零侵入。

---

## 代码接入完成度

| 模块 | 来源 | commit | 验证 |
|---|---|---|---|
| 治理：mewcode 迁移许可 | 基础闭环 | `12d9299` | `docs/迁移记录/mewcode迁移许可说明.md` |
| Glob / Grep / EditFile / Diff 工具 | 基础闭环 | `12d9299` | `test_glob_tool.py`(3) + `test_grep_tool.py`(3) + `test_edit_file_tool.py`(4) + `test_diff_tool.py`(2) |
| OpenAI 兼容 Provider + config 切换 | 基础闭环 | `12d9299` | `test_config_env.py::test_llm_provider_and_openai_base_url_from_toml` |
| Git 工作树模块 + enter/exit 工具 | 基础闭环 | `12d9299` | `test_worktree.py`(3) + `test_worktree_tools.py`(2) |
| Seatbelt / Bubblewrap 沙箱 + BashTool 接入 | 基础闭环 | `12d9299` | `test_sandbox.py`(3) + `test_bash_tool_uses_sandbox_when_provided` |
| PermissionMode + Hooks | 包 D | `34e667a` | 8 任务 271 测试 |
| 压缩熔断 / 检查点 / 长期记忆 | 包 E | `10db6a7` | 7 任务 287 测试（含修复 6 个 `test_compactor.py` pre-existing 失败） |
| 交互工具 + 文件安全 TDD 计划 | 包 C | `f6809ce` | 8 任务 C1-C8 共 1947 行 |
| TUI 状态栏显示 provider/model | 集成后 | `fef3b94` + `38f6b8f` | TUI 顶部状态栏实时显示 `provider/model`（来自 `KamaConfig.llm`） |

---

## 自动化验收数据

- **全量单元测试**：`uv run pytest tests/unit -q` → **329 passed in 21.44s**（基线 256 + 基础闭环新增 24 + 包 D/E/C 配套用例 + TUI 修复回归）
- **类型检查**：`uv run mypy src` → **Success: no issues found in 111 source files**
- **Lint**：`uv run ruff check src tests` → 51 个 pre-existing 警告，**未引入新警告**

---

## LLM Provider 当前方案（核心结论）

**结论：daemon 不需要改任何代码即可切换 Anthropic 兼容端点。**

| 场景 | 配置 | 行为 |
|---|---|---|
| 真实 Anthropic（默认） | `ANTHROPIC_API_KEY=sk-...`（不设 `ANTHROPIC_BASE_URL`） | 走 `https://api.anthropic.com/v1/messages` |
| Anthropic 兼容的第三方（DeepSeek / Moonshot 等） | `ANTHROPIC_API_KEY=sk-...` + `ANTHROPIC_BASE_URL=https://<host>/anthropic` | `anthropic` Python SDK 0.105.2 原生读 `ANTHROPIC_BASE_URL`，base url 透明替换 |
| 模型名 | `KAMA_LLM_DEFAULT_MODEL=deepseek-v4-pro`（TOML 或环境变量） | 写入 `KamaConfig.llm.default_model` → 写入 HTTP body `model` 字段 |

**注意**：`core/llm/factory.py` 同时支持 `provider=openai_compat`（OpenAI 协议路径），用于走 `/v1/chat/completions` 的端点（如 Moonshot 原生 OpenAI）。两条路径并存，按 `llm.provider` 字段路由。

### TUI 状态栏可见性

`kama-tui` 顶部状态栏实时显示 `provider/model`（commit `38f6b8f`），用户在终端可一眼确认当前 LLM 配置：
- `anthropic/claude-sonnet-4-6`（默认）
- `openai_compat/kimi-k3`（Moonshot）
- `anthropic/deepseek-v4-pro`（DeepSeek /anthropic 兼容端点）

---

## 已 commit 的代码（`integration/wave1` 自下而上）

```
38f6b8f fix: kama-tui 入口改为委托 app.run()，修复状态栏 provider/model 不显示
fef3b94 feat: TUI 状态栏显示当前 provider/model
f6809ce merge: 包 C TDD 计划（8 任务 1947 行）
10db6a7 merge: 包 E 7 任务（压缩熔断/检查点/长期记忆）
34e667a merge: 包 D 8 任务（PermissionMode + Hooks）
12d9299 merge: 基础闭环 11 任务（Glob/Grep/EditFile/Diff/OpenAI Provider/Worktree/沙箱）
bc1a204 chore: 解除 core/workspace/ 的 gitignore 误命中
```

---

## 未通过项与后续修复计划

| 项 | 原因 | 修复责任 |
|---|---|---|
| 包 C（C1-C8）8 任务实现 | 仅完成 TDD 计划文档（`docs/superpowers/plans/2026-07-21-kama-agent-package-c-interactive-file-safety.md`），代码未实现 | Wave 2 排期 |
| 包 F（依赖包 D） | 待 Wave 2 | Wave 2 |
| 包 G（独立） | 待 Wave 2 | Wave 2 |

---

## Wave 1 关闭判定

- [x] 4 个子包全部合入 `integration/wave1`
- [x] 自动化测试 329 passed / 0 failed
- [x] mypy 0 issue / 111 files
- [x] TUI 7 步端到端跑通（用户本机）
- [x] Provider 切换方案确定（env var 零代码改动）
- [x] TUI 状态栏显示 provider/model

**Wave 1 验收通过，可进入 Wave 2（包 F + 包 G 并行）。**
